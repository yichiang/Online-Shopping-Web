@using OnlineShoppingWeb.Enities
@model IEnumerable<User>
<h1>List of User In the account</h1>

@foreach (var user in Model)
{
    <h2>@user.UserName</h2>
    <h2>@user.JoinDate</h2>
    @foreach (var role in user.Roles)
    {
        <h2>@role</h2>

    }
}